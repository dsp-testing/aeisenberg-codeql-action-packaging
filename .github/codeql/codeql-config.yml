name: Pack testing in the CodeQL Action

disable-default-queries: true

packs:
  - dsp-testing/cpp-pack # latest
  - dsp-testing/common-cpp-coding-standards
paths:
  - ./cpp-src
paths-ignore:
  - '**/packages'
