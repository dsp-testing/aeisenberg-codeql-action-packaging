name: Pack testing in the CodeQL Action
disable-default-queries: true
queries:
  - name: Run custom javascript queries
    uses: ./.github/queries
packs:
  # - dsp-testing/codeql-pack1@0.0.1
  - dsp-testing/codeql-pack2 # latest
paths:
  - ./src
paths-ignore:
  - '**/packages'
