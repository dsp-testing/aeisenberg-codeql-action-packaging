name: "Other"

on:
  pull_request:

jobs:
  other:
    runs-on: ubuntu-latest

    steps:
      # This should trigger a missing actions permissions alert.
      - name: Checkout repository without permissions
        uses: actions/checkout@main
